<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Bootstrap Directory Structure with JS Plugins</title>
  <!-- Bootstrap CSS -->
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
</head>
<body>
  <div class="container mt-5">
    <h1 class="mb-4 text-primary">Bootstrap Directory Structure</h1>

    <!-- CSS Folder -->
    <div class="card mb-4">
      <div class="card-header bg-info text-white">
        <strong>📁 css/ — Bootstrap Styles</strong>
      </div>
      <div class="card-body">
        <p>Contains compiled CSS files for Bootstrap styling.</p>
        <ul>
          <li><code>bootstrap.css</code> — Full unminified CSS (with comments).</li>
          <li><code>bootstrap.min.css</code> — Minified CSS for production.</li>
          <li><code>bootstrap.rtl.css</code> — Right-to-left (RTL) language support.</li>
          <li><code>bootstrap-grid.css</code>, <code>bootstrap-utilities.css</code> — Optional partial builds.</li>
        </ul>
      </div>
    </div>

    <!-- JS Folder -->
    <div class="card mb-4">
      <div class="card-header bg-success text-white">
        <strong>📁 js/ — Bootstrap JavaScript</strong>
      </div>
      <div class="card-body">
        <p>Includes JavaScript files to enable interactive Bootstrap components.</p>
        <ul>
          <li><code>bootstrap.bundle.js</code> — Bootstrap + Popper.js.</li>
          <li><code>bootstrap.bundle.min.js</code> — Minified version for production.</li>
          <li><code>bootstrap.js</code> — Bootstrap only (no Popper).</li>
          <li><code>bootstrap.min.js</code> — Minified Bootstrap only.</li>
        </ul>
      </div>
    </div>

    <!-- Icons Folder -->
    <div class="card mb-4">
      <div class="card-header bg-warning text-dark">
        <strong>📁 icons/ — Bootstrap Icons (optional)</strong>
      </div>
      <div class="card-body">
        <p>Contains icon fonts or SVGs for use with Bootstrap projects.</p>
        <ul>
          <li><code>bootstrap-icons.css</code> — Stylesheet for using icons.</li>
          <li><code>fonts/</code> or <code>icons/</code> — Directory with SVG or font files.</li>
          <li>Use icons with classes like <code>&lt;i class="bi bi-alarm"&gt;&lt;/i&gt;</code></li>
        </ul>
      </div>
    </div>

    <!-- Example Bootstrap JavaScript Component -->
    <div class="text-center">
      <button class="btn btn-primary" data-bs-toggle="collapse" data-bs-target="#info">Toggle Info</button>
      <div id="info" class="collapse mt-3">
        <div class="alert alert-info">This is a collapsible Bootstrap component using JS plugins!</div>
      </div>
    </div>
  </div>

  <!-- Bootstrap JS (with Popper included) -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
